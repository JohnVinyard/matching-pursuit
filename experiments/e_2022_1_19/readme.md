Can a multi-resolution architecture using FNet for both the encoder and decoder be used as an autoencoder, or at least to overfit?

Learnings:
-----------------
- decoder path seems to work much better without token mixing